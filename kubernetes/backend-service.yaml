apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: ai-platform
  labels:
    app: backend
spec:
  ports:
    - port: 8000
      targetPort: 8000
      name: http
  selector:
    app: backend
  type: ClusterIP
